server:
  port: 8080

spring:
  datasource:
    url: ${DB_URI:jdbc:mysql://localhost:3306/biblioteca?useUnicode=true&characterEncoding=UTF-8&serverTimezone=UTC}
    username: ${DB_USER:biblioteca_user}
    password: ${DB_PASSWORD:biblioteca_pass}
    driver-class-name: ${DB_DRIVER:com.mysql.cj.jdbc.Driver}
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect
